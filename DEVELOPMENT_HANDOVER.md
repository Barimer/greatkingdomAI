# Great Kingdom AI - 개발 히스토리 및 Phase 4 인수인계 가이드 (Development Handover)

이 문서는 다른 컴퓨터로 개발 환경을 이전할 때, 기존의 개발 히스토리와 현재 진행 상태, 그리고 향후 수행해야 할 구체적인 태스크 지침을 제공하기 위해 작성되었습니다.

---

## 1. 📋 프로젝트 개요 및 규칙 제약사항
* **목표**: 그레이트 킹덤(Great Kingdom) 보드게임 규칙 엔진 상에서 작동하는 강력하고 안정적인 알파-베타 미니맥스(Minimax) AI를 개발하고, 향후 강화학습 기저가 될 자가 대국(Self-Play) 데이터를 생성하는 안정적인 환경을 구축합니다.
* **원본 규칙 엄격 유지 (규칙 변경 절대 금지)**:
  * **캡처 즉시 승리**: 단 1개의 돌이라도 캡처당하면 캡처한 쪽이 즉시 서든데스로 승리.
  * **후공 덤(Komi)** 규칙 유지.
  * **중립 성(Neutral Castle)** 규칙 유지.
  * **영토(Territory)** 판정 규칙 유지.
* **AI 개발 및 코드 수정 원칙 (CLI 규칙)**:
  * **기존 레이아웃/디자인/글꼴 보존**: 사용자 지시가 없는 한 기존 화면 레이아웃, UI 디자인, 글꼴 등은 건드리지 않고 최대한 원본을 유지합니다.
  * **시스템 로직 변경 제한**: 단순 보완/수정 단계에서 핵심 시스템 아키텍처나 엔진 룰을 사용자의 명시적 허가 없이 임의로 수정하지 않습니다.

---

## 2. ⏳ 지금까지의 개발 히스토리 (History)

### 2.1. Phase 3: Zobrist Hashing 및 Transposition Table(치환표) 도입
* 탐색 연산 최적화를 위해 64비트 Zobrist 해싱 기법 및 캐싱용 치환표 구조를 구현하여 탐색 속도를 향상했습니다.
* **성능 벤치마크 결과**:
  * **Depth 3**: 턴당 평균 약 15.66초 소요, 1판당 약 7분.
  * **Depth 4**: 턴당 평균 약 119.49초 소요, 1판당 약 55.7분 (BFS 기반 영토 계산 누적으로 속도 지연 확인).

### 2.2. AI vs AI 100판 시뮬레이션 및 병목 해결
* 처음 100판을 실행했을 때, 무작위성이 전혀 없어 **매 판 동일한 45수만 반복**되는 결정론적 루프와 **1판당 38초**의 긴 연산 시간 문제를 발견했습니다.
* **해결 조치**:
  * **멀티프로세싱 병렬화**: `multiprocessing.Pool` 및 `imap_unordered`를 활용해 가용 CPU 코어(11개 프로세스)를 가동하여 연산 속도를 6~7배 가속.
  * **무작위성 추가**: 최적 점수가 동점일 때 임의 선택하는 **Random Tie-breaking** 적용, 첫 번째 착수(Move 001)는 보드 위 빈 칸 중 완전히 **랜덤 착수**하도록 강제.
  * **결과**: 동일 수순 반복 루프를 완벽히 해결했으며, BLUE(선공) 64% vs ORANGE(후공) 36%의 승률 획득 (100% 캡처 종료).

### 2.3. Depth 2 vs Depth 3 대결 및 평가함수 맹점 발견
* **대결 결과**: Depth 3가 **49승 1패(98.0%)**로 압승.
* **분석 결과**:
  * Depth 2는 1수 앞의 직접 캡처만 볼 수 있어, Depth 3가 설계하는 **'3수 앞의 포위 덫(전술)'**을 감지하지 못하고 6~8수 만에 급사함. 향후 모든 실험은 최소 **Depth 3** 기준으로 확정.
  * 이 과정에서 적용된 `Liberty Danger Penalty`가 과도(자유도 3=-20, 2=-100, 1=-500)하여 첫 수가 구석에 배치되면 AI가 활로 점수 감점을 피하기 위해 대국을 조기 포기하고 **패스(Pass)를 연타해 게임을 끝내는 Surrender Loop 기현상**이 발생함.

---

## 3. 🏃 현재 진행 중인 작업 (Active Context)

### 3.1. 평가함수 가중치 재조정 완료
과도한 페널티로 인한 조기 패스 Surrender 루프를 막기 위해 다음과 같이 가중치를 재조정했습니다.
* **Liberty Danger Penalty (자유도 위기 감점)**:
  * 자신의 그룹 자유도가 **3 이하**: `-5`
  * 자신의 그룹 자유도가 **2 이하**: `-30`
  * 자신의 그룹 자유도가 **1 이하 (단수)**: `-150`
* **Opponent Threat Bonus (상대 위협 가산점)**:
  * 상대 그룹 자유도가 2 이하일 때 **`+20`** 부여 (공격 보너스가 방어 감점 패널티인 `-30`보다 과도하지 않도록 조율).

### 3.2. Depth 3 vs Depth 3 재검증 100판 시뮬레이션 구동 중
* **스크립트**: `simulate_d3_benchmark.py`
* **백그라운드 태스크**: `task-582` (11개 프로세스 풀가동)
* **목표**: 완화된 평가함수 룰 하에서 100% 캡처 종료 양상이 변화하는지, 조기 패스 종료 현상이 사라졌는지, 그리고 Depth 3 간의 균형 잡힌 대국이 성립하는지 검증합니다.
* **현재 관측 결과**: 수순이 8수, 13수, 20수 등으로 다양하게 분산 중이며, 패스 조기 종료 없이 캡처 공방이 팽팽하게 이루어지고 있음이 확인되었습니다.

---

## 4. 🚀 다음 컴퓨터에서 수행할 작업 (Phase 4 후속 태스크)

다른 컴퓨터에서 소스 코드를 pull 한 뒤 아래 순서로 작업을 이어나가면 됩니다.

### [작업 3 완수] Depth 3 100판 재검증 결과 확인
* 현재 실행 중인 `simulate_d3_benchmark.py`가 완료되면 출력되는 8가지 지표를 분석합니다.
  1. BLUE 승률 / 2. ORANGE 승률 / 3. Capture 종료 횟수 / 4. Territory 종료 횟수 / 5. 평균 수순 / 6. 최소 수순 / 7. 최대 수순 / 8. PASS 조기 종료 횟수
* 만약 PASS 조기 종료가 0에 수렴하고 정상적인 캡처 및 영토 장기전이 균형 있게 어우러진다면 **평가함수가 안정화**된 것으로 판정합니다.

### [작업 4] Self-Play 500판 생성 스크립트 작성 및 구동
* **규모**: Depth 3 vs Depth 3 대국 500판.
* **저장 경로**: `data/games` 폴더 생성 후 대국 결과를 개별 JSON 파일로 저장.
* **저장 포맷**:
  ```json
  {
    "winner": 1,
    "win_reason": "CAPTURE", // 또는 "TERRITORY"
    "moves": [
      [4, 2],
      [3, 3]
    ]
  }
  ```

### [작업 5] Self-Play 데이터 품질 분석
* 생성된 500판 JSON 데이터를 파싱하여 최종 통계를 도출합니다.
  * 승률, Capture 비율, Territory 비율, 평균 수순, 첫 수(Move 001) TOP 10 좌표 분포, 중복 대국 비율.

### [작업 6] 차기 단계 (Depth 4) 설계 조사
* Self-Play 500판까지 완료되면, Zobrist Hashing 및 치환표를 연계하여 **Depth 4 탐색이 실시간 성능상 구현 가능한지 여부를 조사**하고 성능 추정 리포트를 작성합니다 (이 단계에서는 설계만 수행하고 코드는 구현하지 말 것).

---

## 💻 실행 및 연동 가이드
1. **저장소 가져오기**:
   ```bash
   git clone https://github.com/Barimer/greatkingdomAI.git
   ```
2. **벤치마크 및 시뮬레이션 명령어**:
   * 재검증 100판 기동 (현재 실행 중인 스크립트):
     ```bash
     python simulate_d3_benchmark.py
     ```
   * 500판 생성 스크립트를 새로 작성하여 기동할 때도 `multiprocessing` 패킹 구조를 반드시 유지해야 15~20배 빠르게 데이터를 수집할 수 있습니다.
